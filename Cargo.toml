[package]
name = "monitor_table"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1"
dfsql = { version = "0.10", default-features = false }
hdv = { version = "0.1", optional = true }
math = { git = "https://github.com/Banyc/math.git", tag = "v0.0.8" }
polars = { version = "0.40", features = ["lazy"] }
slotmap = "1"

[features]
default = []
