[package]
name = "monitor_table"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1"
dfsql = { version = "0.11", default-features = false }
hdv = { version = "0.2", optional = true }
math = { git = "https://github.com/Banyc/math.git", tag = "v0.0.9" }
polars = { version = "0.41", features = ["lazy"] }
slotmap = "1"

[features]
default = []
